# Diplom_2

```
1. test_create_user() - проверяет, что можно создать нового пользователя
```

```
2. test_create_exist_user() - проверяет, что нельзя создать пользователя, который уже зарегистрирован
```

```
3. test_create_user_without_required_fields() - проверяет, что нельзя создать пользователя, не заполнив обязательные поля
```

```
4. test_login_user() - проверяет, что можно войти под существующим пользователем
```

```
5. test_login_user_with_invalid_password() - проверяет, что нельзя войти, указав неверный пароль, но верный email
```

```
6. test_login_user_with_invalid_email() - проверяет, что нельзя войти, указав неверный email, но верный пароль
```

```
7. test_update_email() - проверяет, что можно изменить email пользователя в профиле
```

```
8. test_update_name() -  проверяет, что можно изменить имя пользователя в профиле
```

```
9. test_update_email_negative() - проверяет, что нельзя изменить email пользователя без авторизации
```

```
10. test_update_name_negative() - проверяет, что нельзя изменить имя пользователя без авторизации
```

```
11. test_create_order() - проверяет, что можно создать заказ
```

```
12. test_create_order_without_ingredients() - проверяет, что нельзя создать заказ, не выбрав ингредиенты
```

```
13. test_create_order_without_token() - проверяет, что нельзя создать заказ без авторизации
```

```
14. test_create_order_with_invalid_ingredients() - проверяет, что нельзя создать заказ, если подать неверный хеш ингредиента
```

```
15. test_get_orders() - проверяет, что можно получить заказ пользователя
```

```
16. test_get_orders_without_token() - проверяет, что нельзя получить заказ пользователя без авторизации
```
